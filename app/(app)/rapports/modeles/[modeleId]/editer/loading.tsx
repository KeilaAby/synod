import { ReportEditorSkeleton } from '@/components/skeletons';

/** UI-15 — trois panneaux, comme l'éditeur : palette, composition, réglages. */
export default function Loading() {
  return <ReportEditorSkeleton />;
}
