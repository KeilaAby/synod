import { DetailSkeleton } from '@/components/skeletons';

/** UI-15 — squelette de fiche : en-tete, carte laterale, panneau de donnees. */
export default function Loading() {
  return <DetailSkeleton />;
}
